# project.config - Central Configuration for SaaS Factory Template
# =============================================================================
# This file is the SINGLE SOURCE OF TRUTH for project naming and configuration.
# Used by: spawn-project.sh, docker-compose, terraform
#
# When spawning a new project, only this file needs to be customized.
# =============================================================================

# -----------------------------------------------------------------------------
# PROJECT IDENTITY
# -----------------------------------------------------------------------------
# These values define the project name used across all systems.
# Use lowercase letters, numbers, and hyphens only.

PROJECT_NAME=gst-buddy
PROJECT_DISPLAY_NAME="GstBuddy"
PROJECT_DESCRIPTION="Multi-tenant SaaS Factory Template"

# -----------------------------------------------------------------------------
# JAVA / MAVEN
# -----------------------------------------------------------------------------
# Maven coordinates for the project

JAVA_GROUP_ID=com.learning
JAVA_ARTIFACT_ID=CloudInfraLite
JAVA_PACKAGE_BASE=com.learning

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# PostgreSQL configuration
# Note: In multi-tenant mode, each tenant gets their own schema within this DB

DB_NAME=saas_db
DB_USER=postgres
DB_PORT=5432

# -----------------------------------------------------------------------------
# DOCKER
# -----------------------------------------------------------------------------
# Docker network and volume naming

DOCKER_NETWORK=saas-network
DOCKER_VOLUME_PREFIX=${PROJECT_NAME}
DOCKER_COMPOSE_PROJECT_NAME=${PROJECT_NAME}

# -----------------------------------------------------------------------------
# AWS / TERRAFORM
# -----------------------------------------------------------------------------
# AWS resource naming and SSM parameter paths

AWS_REGION=us-east-1
SSM_PREFIX=/${PROJECT_NAME}

# Environment-specific project names (for resource isolation)
# Budget environment uses a suffix to avoid conflicts with dev
PROJECT_NAME_BUDGET=${PROJECT_NAME}-budget
PROJECT_NAME_PROD=${PROJECT_NAME}-prod

# -----------------------------------------------------------------------------
# COGNITO
# -----------------------------------------------------------------------------
# AWS Cognito user pool naming

COGNITO_POOL_NAME="${PROJECT_DISPLAY_NAME}"
COGNITO_DOMAIN_PREFIX=${PROJECT_NAME}

# -----------------------------------------------------------------------------
# SERVICE PORTS
# -----------------------------------------------------------------------------
# Default ports for each service (used in docker-compose and application.yml)

PORT_EUREKA=8761
PORT_GATEWAY=8080
PORT_AUTH=8081
PORT_BACKEND=8082
PORT_PLATFORM=8083

# -----------------------------------------------------------------------------
# FRONTEND
# -----------------------------------------------------------------------------
# Angular frontend configuration

FRONTEND_PORT=4200
FRONTEND_APP_NAME=frontend
