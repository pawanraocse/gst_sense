# AWS OTEL Collector customization - currently using base image as-is
# Note: Healthcheck is disabled in docker-compose because the minimal base image
# lacks wget/curl/shell executables needed for HTTP health checks.

FROM amazon/aws-otel-collector:latest
